// @flow
import { useEffect } from 'react';

const useGoogleApi = (key: string): Object => {
  const googleObject = { key };
  // const [loaded, setIsLoaded] = useState(false);

  useEffect(() => {
    // todo
  });

  return googleObject;
};

export default useGoogleApi;
